<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.green.gittest.user.UserMapper">

    <insert id="postUser" useGeneratedKeys="true" keyProperty="userId">
        INSERT INTO user
        SET uid = #{uid}
        , email = #{email}
        , upw = #{upw}
        , nickname = #{nickname}
        , profile_pic = #{profilePic}
    </insert>

    <select id="getUserById">
        SELECT user_id AS userId, uid, email, upw, nickname
        , profile_pic AS profilePic, sign_up_date AS signUpDate
        , profile_update AS profileUpdate
        FROM user
        WHERE uid = #{uid}
    </select>

    <update id="patchPassword">
        UPDATE user
        SET upw = #{newPw}
        WHERE user_id = #{userId}
    </update>

</mapper>